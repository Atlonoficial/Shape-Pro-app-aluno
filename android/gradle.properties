# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# ============================================================
# OTIMIZAÇÕES DE BUILD
# ============================================================

# Aumentar memória para builds otimizados (ProGuard/R8)
org.gradle.jvmargs=-Xmx4096m -XX:MaxPermSize=512m -XX:+HeapDumpOnOutOfMemoryError

# Builds paralelos e caching para performance
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configureondemand=true

# AndroidX
android.useAndroidX=true
android.enableJetifier=true

# Build features
android.defaults.buildfeatures.buildconfig=true
android.nonTransitiveRClass=false
android.nonFinalResIds=false

# ============================================================
# APP SIGNING (RELEASE) - CONFIGURE ANTES DE GERAR AAB/APK
# ============================================================
# INSTRUÇÕES:
# 1. Gere keystore: keytool -genkey -v -keystore android/app/shape-pro-release.keystore -alias shape-pro -keyalg RSA -keysize 2048 -validity 10000
# 2. Descomente as linhas abaixo e preencha com suas credenciais
# 3. NUNCA commite este arquivo com senhas no Git!

# RELEASE_STORE_FILE=shape-pro-release.keystore
# RELEASE_STORE_PASSWORD=SUA_SENHA_AQUI
# RELEASE_KEY_ALIAS=shape-pro
# RELEASE_KEY_PASSWORD=SUA_SENHA_AQUI
